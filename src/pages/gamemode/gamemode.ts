import { Component } from '@angular/core';
import { NavController } from 'ionic-angular';

@Component({
  selector: 'page-gamemode',
  templateUrl: 'gamemode.html'
})
export class GamemodePage {

  constructor(public navCtrl: NavController) {
  }
  
}
